export function lsRead(key,seed){const raw=localStorage.getItem(key);if(!raw){const s=seed();localStorage.setItem(key,JSON.stringify(s));return s}try{return JSON.parse(raw)}catch{return seed()}}export function lsWrite(key,v){localStorage.setItem(key,JSON.stringify(v))}export function makeId(){return Math.random().toString(36).slice(2,10)}export function nowISO(){return new Date().toISOString()}